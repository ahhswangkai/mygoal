# 🎯 足彩计算器 - 前后端分离版

## 项目架构

```
mygoal/
├── web_app.py              # 后端Flask服务 (端口 5002)
├── frontend/               # 前端Vue项目 (端口 3000)
│   ├── package.json
│   ├── vite.config.js
│   ├── index.html
│   └── src/
│       ├── App.vue        # 主应用
│       ├── style.css      # 样式（精准还原体彩）
│       └── components/
│           ├── HadHhad.vue    # 胜平负/让球
│           ├── Score.vue      # 比分
│           ├── Goals.vue      # 总进球
│           └── Hafu.vue       # 半全场
├── templates/             # 原后端模板（兼容保留）
└── requirements.txt
```

## 快速启动

### 方式一：分别启动（推荐开发模式）

#### 1️⃣ 启动后端 (新终端)
```bash
cd /Users/wang/workspace/mygoal
source venv/bin/activate
pip install flask-cors  # 首次需要安装
python web_app.py
```

后端将在 http://127.0.0.1:5002 启动

#### 2️⃣ 启动前端 (新终端)
```bash
cd /Users/wang/workspace/mygoal/frontend
npm install  # 首次需要安装依赖
npm run dev
```

前端将在 http://localhost:3000 启动

---

### 方式二：使用启动脚本

```bash
# 后端
cd /Users/wang/workspace/mygoal
source venv/bin/activate
python web_app.py

# 前端（新终端）
cd /Users/wang/workspace/mygoal/frontend
./start.sh
```

## 功能特点

✅ **5种玩法全覆盖**
- 胜平负 + 让球胜平负
- 比分
- 总进球
- 半全场
- 混合过关

✅ **智能计算**
- 实时计算总赔率
- 理论最高奖金
- 倍数/金额自由设置

✅ **界面还原**
- 精准还原体彩红色主题
- 移动端优先设计
- 流畅的交互动画

✅ **前后端分离**
- Vue3 + Vite 前端
- Flask 后端
- RESTful API 接口

## API 接口

### 获取比赛数据
```
GET /api/calc/matches
```

响应示例:
```json
{
  "success": true,
  "data": [
    {
      "id": "match_0",
      "num": "周三101",
      "league": "世界杯",
      "homeTeam": "英格兰",
      "awayTeam": "阿根廷",
      "handicap": -1,
      "had": { "win": 2.36, "draw": 2.75, "lose": 2.92 },
      "hhad": { "win": 5.65, "draw": 3.80, "lose": 1.45 },
      "score": { ... },
      "goals": { ... },
      "hafu": { ... }
    }
  ]
}
```

## 技术栈

### 前端
- Vue 3 (Composition API)
- Vite 5
- CSS3 (原生，无UI库)

### 后端
- Flask 2.0
- Flask-CORS
- MongoDB (可选)

## 开发说明

### 前端代理配置
在 `vite.config.js` 中已配置代理：
```javascript
proxy: {
  '/api': {
    target: 'http://127.0.0.1:5002',
    changeOrigin: true
  }
}
```
这样前端请求 `/api/calc/matches` 会自动转发到后端。

### 添加新玩法
1. 在 `frontend/src/components/` 下创建新组件
2. 在 `App.vue` 中添加新的 `tab`
3. 后端API补充对应数据

## 访问导航

| 页面 | 地址 |
|------|------|
| 🎯 计算器(Vue新版) | http://localhost:3000 |
| 🏠 原首页 | http://127.0.0.1:5002 |
| 📊 原统计 | http://127.0.0.1:5002/stats |
| 🧮 原计算器(兼容) | http://127.0.0.1:5002/calculator |
