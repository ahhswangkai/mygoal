---
name: model-calibration
description: 使用现有本地预测作为校准证据并限制过度自信
version: 1.0.0
priority: 80
requires_any:
  - prediction
---
# 校准规则

1. 本地预测是规则模型结果，只能作为一项证据。
2. 当本地预测、近期状态和赔率方向一致时，可以适度提高置信度。
3. 任意两类核心信号冲突时，必须降低置信度并写入 risks。
4. 不得照抄本地置信度；最终置信度上限为 85。
5. 如果输入没有历史复盘准确率，不得声称模型长期胜率。
